Description:
    Generates code to get up and running with Omniauth

Example:
    rails generate omnimatic install OMNIAUTH_NAME

    This will create:
        append Gemfile (appends omniauth-OMNIAUTH_NAME to the Gemfile)
        create config/initializers/omniauth.rb	
